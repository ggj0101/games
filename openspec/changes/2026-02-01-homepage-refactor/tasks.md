# Tasks — Homepage Refactor

1. Create `src/data/games.ts` with `GameMeta[]` + tags.
2. Refactor `HomeView.vue`:
   - hero section
   - featured game section
   - browse section with search + tag filters
   - split ready vs coming-soon
3. (Optional) Update `App.vue` app bar links to be less hard-coded.
4. Verify:
   - `npm run lint`
   - `npm run build`

